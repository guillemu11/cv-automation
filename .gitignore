# Secrets
.env
.env.local
*.token.json
token.json
credentials.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Data (runtime state — seen_jobs and cache are committed by the bot via CI)
# But local scratch output is ignored
data/generated/
data/tmp/
logs/*.log
logs/*.json

# Firecrawl cache
.firecrawl/

# Playwright persistent profile (login cookies, localStorage — never commit)
data/playwright_profile/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Generated artifacts: output/ IS versioned (private repo) so Paula's clone gets
# every deliverable, landing and outreach pack without a side transfer. Only the
# Office lock files and editor backups inside it are ignored.
*.docx.bak
~$*

# Brainstorming visual-companion scratch
.superpowers/

# NOTE: Paula's source CV and the master plan are versioned too (private repo).
# Keep .env OUT of git (secrets) — it is the only thing transferred separately.

# Third-party reference decks dropped in for inspiration (not ours to publish)
PROPUESTA ABSOLUT HELENA DE ALBA(pdfgear.com).pdf

# LLM Wiki — raw sources (copyright-sensitive, heavy)
wiki/raw/
# Obsidian local state (but NOT the whole .obsidian/ — plugins/config are versioned)
wiki/.obsidian/workspace*
wiki/.obsidian/cache
wiki/.obsidian/graph.json
